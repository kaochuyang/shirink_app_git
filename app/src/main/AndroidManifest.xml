<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.com.cct.ms2.shirink_app_git">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tod_setting" />
        <activity android:name=".plan_setting" />
        <activity
            android:name=".Main3Activity"
            android:label="@string/title_activity_main3"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".plan_detail" />
        <activity
            android:name=".step_setting"
            android:label="@string/title_activity_step_setting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".V3MessageActivity" />
        <activity
            android:name=".Direction_setting"
            android:label="@string/title_activity_direction_setting"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>